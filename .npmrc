# Enforce the Node engine constraint from package.json at install time, in
# addition to the preinstall guard (scripts/check-node.mjs). Node 20.9+ is
# required (sharp / @supabase/supabase-js); Node 18 is End-of-Life.
engine-strict=true
